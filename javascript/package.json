{
  "name": "build-tools",
  "private": true,
  "workspaces": [
    "*"
  ],
  "scripts": {
    "build:ordered": "npm -w @datalackey/tooling-core run build && npm -w @datalackey/nx-graph-to-mermaid run build",
    "test:all": "npm -ws test --if-present",
    "build": "npm run build:ordered && npm run test:all"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@datalackey/update-markdown-toc": "^1.1.5",
    "nx": "22.5.4",
    "vitest": "^4.0.18"
  }
}
