TOC generation engine: parses Markdown headings (skipping fenced code blocks), generates GitHub-slugged anchor links, and injects the result between `<!-- TOC:START -->` / `<!-- TOC:END -->` markers.
